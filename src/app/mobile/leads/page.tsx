"use client";

// Placeholder. Real lead-request UI rebuilt from claude.ai/design template.

export default function MobileLeadsPage() {
  return (
    <main style={{ padding: 24, fontFamily: "var(--sans)", color: "var(--ink-2)" }}>
      <h1 style={{ fontFamily: "var(--serif)", fontSize: 24 }}>Richiedi avvocato</h1>
      <p>Form richiesta lead. UI in arrivo.</p>
    </main>
  );
}
