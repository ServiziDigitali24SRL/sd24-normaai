"use client";

// Placeholder. Real chat UI (with Sofia voice + chat input) rebuilt from
// claude.ai/design template, integrating useMobileVoice + useAgentStream.

export default function MobileChatPage() {
  return (
    <main style={{ padding: 24, fontFamily: "var(--sans)", color: "var(--ink-2)" }}>
      <h1 style={{ fontFamily: "var(--serif)", fontSize: 24 }}>Chat con Sofia</h1>
      <p>Chat mobile. UI in arrivo dal nuovo template.</p>
    </main>
  );
}
