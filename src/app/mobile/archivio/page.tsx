"use client";

// Placeholder. Real archive UI rebuilt from claude.ai/design template.

export default function MobileArchivePage() {
  return (
    <main style={{ padding: 24, fontFamily: "var(--sans)", color: "var(--ink-2)" }}>
      <h1 style={{ fontFamily: "var(--serif)", fontSize: 24 }}>Archivio</h1>
      <p>Storico conversazioni. UI in arrivo.</p>
    </main>
  );
}
